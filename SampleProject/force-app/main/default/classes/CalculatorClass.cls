public with sharing class CalculatorClass {
    public decimal addThreeNumbers(integer a , integer b,integer c){
        return a+b+c;
    }
    public decimal mutiplythreenumbers(integer a, integer b, integer c){
        return a*b*c;
    }
}